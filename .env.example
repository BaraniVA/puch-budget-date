# Copy to .env and fill in your values
PORT=3000
GEMINI_API_KEY=
OWNER_PHONE=919876543210
# Optional: Comma-separated bearer token to phone mappings (digits only, no '+')
# Example: VALIDATE_TOKEN_MAP=alpha:919876543210,bravo:14155550123
VALIDATE_TOKEN_MAP=
# Public HTTPS base URL of your deployed or tunneled server (no trailing slash)
# For local dev with a tunnel, set this to your tunnel URL (e.g., https://abc123.ngrok-free.app)
BASE_URL=https://your-domain.example.com
